# Function test
fn add(a, b) {
    return a + b
}

result = add(10, 20)
print result

# Recursive fibonacci
fn fib(n) {
    if n < 2 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

print fib(10)
